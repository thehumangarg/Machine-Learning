import pandas as pd
import numpy as np
dataframe:
data={

      key value pairs like json data

      }
               df1= pd.DataFrame(data)
setting index: df2= pd.DataFrame(data, index=['a', 'b', 'c', 'd'])
extracting info: df2.loc['a']            df2.iloc[:, -1]
